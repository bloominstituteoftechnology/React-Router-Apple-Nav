import React from 'react';
import PropTypes from 'prop-tyles';

const SubNav = props => {};

SubNav.propTypes = {
  link: PropTypes.shape({
    name: string,
    image: string
  })
};

export default SubNav;